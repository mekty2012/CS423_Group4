(ns batchreader
  (:import (java.io BufferedReader FileReader))
  (:require [gorilla-plot.core :as plot]))
(use 'nstools.ns)
(ns+ template
  (:like anglican-user.worksheet))
(defn Example []
  (with-open [rdr (clojure.java.io/reader "worksheets/CS423_Group4/cifar-10-batches-bin/data_batch_1.bin")]
    rdr
    )
  )
(defn process-file [file-name]
  (with-open [rdr (BufferedReader. (FileReader. file-name))]
    (doseq [line (line-seq rdr)] (println line))))

; (process-file "worksheets/CS423_Group4/cifar-10-batches-bin/data_batch_1.bin")

